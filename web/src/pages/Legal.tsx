import { Link } from 'react-router-dom'
import { ui } from '../i18n'
import { LangSwitch } from './Shell'

// ⚠️ ЧЕРНОВИК-РЫБА. Тексты НЕ проверены юристом и НЕ являются юридически
// корректными. Для медданных (спец. категория ПДн по 152-ФЗ) формулировки
// согласия регламентированы — перед продом заменить на вычитанные юристом.
// Версия документов задаётся на бэке (settings.terms_version) и фиксируется
// на учётке при регистрации.
export default function Legal() {
  return (
    <div className="auth legal">
      <h1>{ui('Медкарта')} <LangSwitch /></h1>
      <div className="panel">
        <p className="alert">{ui('ЧЕРНОВИК. Документы не проверены юристом и не имеют юридической силы — заглушка до подготовки итоговых текстов.')}</p>

        <h2>{ui('Пользовательское соглашение')}</h2>
        <p className="muted">{ui('Здесь будет описание правил пользования сервисом: назначение, обязанности сторон, ограничение ответственности, порядок изменения условий.')}</p>

        <h2>{ui('Согласие на обработку персональных данных')}</h2>
        <p className="muted">{ui('Здесь будет согласие на обработку персональных данных, включая сведения о состоянии здоровья (специальная категория). Должно содержать: оператора и его реквизиты, перечень данных, цели, действия с данными, срок хранения, порядок отзыва согласия и права субъекта.')}</p>

        <h2>{ui('Политика конфиденциальности')}</h2>
        <p className="muted">{ui('Здесь будет описание, какие данные собираются, как хранятся и защищаются, кому и на каком основании передаются, как их посмотреть или удалить.')}</p>

        <p><Link to="/register">{ui('← к регистрации')}</Link></p>
      </div>
    </div>
  )
}
